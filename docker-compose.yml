version: "3.9"

services:
  server:
    image: creator-sync-server:latest
    build: ./
    env_file: ./.env
    environment:
      SERVICE: server
    depends_on:
      - redis  

  media-service:
    image: creator-sync-server:latest
    build: ./
    env_file: ./.env
    environment: 
      SERVICE: media-service
    depends_on:
      - redis

  redis:
    image: redis:7
    ports:
      - "6379:6379"
